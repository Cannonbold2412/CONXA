# Thin Conxa cloud SaaS dependencies.
# The shared foundation (config, db, models, storage) is installed separately
# from ../../packages/conxa-core by build.sh — it brings pydantic, pydantic-settings
# and SQLAlchemy. Recording/compiling/building deps (playwright, Pillow, lxml, …)
# now live with the Build Studio, not here.

# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# Auth / billing
PyJWT[crypto]>=2.8.0
stripe>=11.0.0
razorpay>=1.4.0

# Postgres driver (db.py uses SQLAlchemy + psycopg2 in production)
psycopg2-binary>=2.9.0
