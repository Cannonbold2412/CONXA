# Fix Log

> Rotated daily into `docs/archive/fix-log/` — see [INDEX.md](docs/archive/fix-log/INDEX.md) for older entries.

---

## Removed the Record page and restructured the Studio around Workflows — 2026-08-12
We used to make you click "Record" to get to your workflows, then "New Workflow" to record one. Now the Workflow list is the first page you see after logging in — you choose an existing workflow to run it, or hit "New" to record a fresh one. Login takes you straight to your workflows; the separate Record page is gone. Behind the scenes, we simplified the data model: a Workflow now holds one recording each, and all workflows in your workspace share a single built package that the runtime downloads once at startup — so adding a second workflow to a company costs almost nothing instead of duplicating the entire installation. The old `/api/v1/plugins/` API routes became `/api/v1/workflows/`, and already-installed customer runtimes using the old routes will break until they reinstall — that's a known tradeoff we made on purpose to keep things clean instead of maintaining two decades of backwards-compatible wiring.
 — 2026-08-12

## Added an honest side-by-side against the alternatives — 2026-08-12
Buyers always ask "why not just use RPA, or Zapier, or build the API, or point an AI agent at it?" — and the homepage made them work that out for themselves. There is now a straight comparison against the five things people actually consider instead of us, showing which of them can be set up without engineers, which need the software vendor's help, which give the same result every time, which survive the screens changing, and which can be sent to your customers' machines.
Every alternative gives up at least two of those, and we do not. Crucially, each one also gets a line saying where it genuinely beats us — because a comparison where one side wins everything gets dismissed as advertising, and because our own sales guidance says naming a rival's strength is what makes people believe the rest. It closes by saying plainly that if all your systems have decent connections already, you should use one of the cheaper tools instead. It replaced three older boxes that made a weaker version of the same point.
 — 2026-08-12

## Anyone clicking "Pricing" was being asked to sign in first — 2026-08-12
Our pricing page was built to be public, but the front door had never been unlocked for it. Anyone who clicked Pricing in the menu, followed the link in the footer, or found the page through a search engine was bounced to a sign-in screen instead — so the one page that answers "what does this cost?" was only visible to people who had already signed up. It is now open to everyone, as intended. Two smaller things on the same page: our top Enterprise plan was advertising "0 seats" and "0 machines" (an internal placeholder meaning "whatever we agree in your contract" that was leaking onto the page as a number), and things a plan does *not* include were shown with a tick beside them, which read as though they were included.
 — 2026-08-12

## Rebuilt the homepage around what we sell and what it costs — 2026-08-12
A few days ago we rewrote our pricing page around one simple story: prove it works on one computer, then run it across your team, then ship it to your own customers. The homepage never got that message, and showed no price at all — so the first page nearly every visitor sees was still selling the old idea and made them hunt for the cost. The homepage now opens with the business result rather than the technology, lays out those four steps up front, and shows all four plans with real prices on the page itself.
The page also got shorter and more convincing. It had twelve sections, and several of them made the same argument twice, so those were merged into one. The three hand-drawn sketches that used to illustrate reliability were replaced with the genuine charts from our customer dashboard — including a real-looking run where one step's button had moved and the automation found it again by itself, which is the single hardest thing to explain in words. Those figures are clearly marked as examples, not customer results, because we do not have enough live usage yet to claim otherwise.
 — 2026-08-12

## Rewrote the whole middle of the product handbook — what we sell, what it does, who we beat, how we make money — 2026-08-12
The five chapters covering the product itself had grown into feature lists that told a reader what exists without telling them why any of it matters. All five were rewritten. The chapter on how the product is built now leads with who owns which piece and why that split is what lets us promise that a customer's data never leaves their own computer. The capabilities chapter now follows a piece of work from being recorded to being used, and explains the thing that most sets us apart: the instructions for finding a button on screen are worked out by fixed rules, not guessed at by an AI, so the same recording always produces the same result — which is what a bank needs before it will trust anything.
The competitor chapter gained a comparison table, an entry for the tools people most often ask us about (the ones that connect apps together automatically), and an honest paragraph under each rival saying where they genuinely beat us — a salesperson who can name a competitor's strength is trusted on everything else they say. The money chapter now spells out the one idea behind our prices: we charge for how far your work travels, never for how often it runs, because running it costs us nothing. It also names the two ways that model could go wrong for us instead of pretending it can't.
Finally, the pre-sale checklist was rebuilt into something anyone can run in a first meeting: five things that immediately rule a job out, seven that decide whether it's worth doing at all, and a firm rule that when a job fails the checklist we say so and walk away rather than trying to force it.
One correction: the handbook still described our installer as containing the customer's tasks inside it. It hasn't for a while — the installer is deliberately near-empty and everything downloads on first use, which is why first-time setup and later updates now work the same way and can't disagree.
 — 2026-08-12

## Wrote a proper "who we sell to" section, including who we should turn away — 2026-08-12
Our handbook described our three types of customer but never said how to tell, on a first call, whether a company is one of them. It now opens with six checks that decide it in minutes — does their work span several systems, does it repeat often enough to be worth teaching, is anyone actually allowed to do it, and so on. Each of the three customer types now names who champions it, who signs the cheque, who can block it, and what "we've won this account" looks like. It also lists, plainly, the companies and situations we should walk away from, so a salesperson loses a bad deal in week one instead of week six. One size estimate is openly labelled a guess rather than a proven pattern, because we have had one pilot, not thirty.
 — 2026-08-12

## Rewrote the "what you get" section of the product handbook — 2026-08-12
Right after the problem section sits our answer to "so what do I actually get out of this?" — and it read like a list of features rather than a promise. It now opens with a single sentence anyone can repeat after one meeting, then spells out what each of our three kinds of buyers gains, and finishes with the four reasons the promise is believable: it survives the software changing, it costs nothing extra no matter how much you use it, sensitive data never leaves the customer's own computer, and nobody has to get permission from the software makers involved. It also states plainly where we are not a fit, so a sales conversation disqualifies the wrong deal in week one instead of week six.
 — 2026-08-12

## Rewrote the "problem" section of the product handbook so it lands with the people we sell to — 2026-08-12
The section explaining what problem Conxa solves was accurate but wandered — it stated the pain in general terms and never connected it to the three kinds of buyers we actually approach. It now walks a reader through the story in order: the work that crosses five systems and belongs to nobody, why no one ever funds the fix, why having a smart AI assistant on every desk made the gap more obvious rather than smaller, and what each of our three buyers is losing today because of it. Anyone writing a pitch, a landing page, or an investor deck can now lift the argument straight off the page.
 — 2026-08-12

## Free installers no longer lock to one computer, and their name and icon now depend on the plan — 2026-08-09
Free trial installers used to be welded to whichever computer built them — copy the installer to a new machine, or even reinstall after a repair, and it would simply refuse to run. That was too strict for normal, honest use, so it's been removed. The separate limit on how many computers a company can use to build with is unaffected and still works the same as before.
In its place, installers now look different depending on the plan. A free installer gets a random, unbranded file name (like a string of random letters) and no custom icon. A paying customer's installer can be named after their own company domain (typed in by them for now — verifying they actually own that domain is a follow-up piece of work) and can carry a custom icon.
 — 2026-08-09

## Developers can skip the sign-in screen when working on the Build Studio locally — 2026-08-09
Launching the Build Studio in developer mode always showed the same "Sign in" screen as the real app, even though a developer just wants to poke at the interface, not log into a live account. Now there's an opt-in developer switch that skips straight past sign-in with a placeholder developer identity, so no browser pop-up or real account is needed. It only works in the isolated developer mode and has to be turned on deliberately, so it can never accidentally show up for a real customer.
 — 2026-08-09

## Support can now grant someone a paid plan for free, with an optional expiry date — 2026-08-09
Until now, the only way a customer got Starter or Pro access was by actually paying through checkout — there was no way for us to comp a plan for a sales demo, a support fix, or a trial extension without either a real charge or someone hand-editing the database. Now there's a proper backend switch: give a specific customer Starter, Pro, or Enterprise access directly, and optionally set how long the grant should last — 1 month, 3 months, a year, whatever's needed. When a timed grant runs out, the customer automatically drops back to the free plan on its own; nobody has to remember to go take it away.
While building this we also found and fixed a small pre-existing bug: if a customer published two automations in the same publish action, the system could occasionally get confused about which one was actually older when deciding what to lock during a downgrade — like a filing system that sometimes misfiles same-day paperwork. Automations now keep a precise creation order internally, so the oldest-gets-locked-first rule always picks the right one.
 — 2026-08-09

## Downgrading a plan now locks the extra workflows you built while on the higher plan — 2026-08-09
Earlier today we found that someone could pay for the top-tier "Pro" plan for a month, build 500 automations, then drop to the cheaper "Starter" plan and quietly keep all 500 running forever, even though Starter is only supposed to allow 200. Nothing was reclaiming that extra capacity — a downgrade only stopped you from building *new* ones past the limit, it never touched what you'd already built.
Now it does. Every automation a company builds is tracked on a permanent list, oldest to newest. If a company ever has more automations than their current plan allows, the oldest ones over the limit get automatically locked — not deleted, just paused — while the newest ones (up to the plan's limit) stay live. Locking is one-way reversible: upgrade again and the locked ones reopen automatically, oldest first, no manual work needed on either side. Locked automations also can't be republished with a new version while they're locked, and a company already at its limit can't sneak in a brand-new automation either — both require freeing up room or upgrading. Importantly, this only affects what the SaaS company can manage going forward; any of *their* customers who already had one of these automations installed keep using it uninterrupted, so a plan change on the company's side never breaks something a real end customer is actively relying on.
 — 2026-08-09

## Checked what happens when someone downgrades their plan after using the higher tier for a while — 2026-08-09
We looked into whether someone could pay for the top-tier "Pro" plan for a month or two, use everything it unlocks, then drop back down to a cheaper plan and quietly keep the extra value. Good news: for the things billed by usage — AI credits, premium features, how far a workflow can be distributed — the system checks your *current* plan every single time, so a downgrade takes effect immediately, the same day. Nobody can bank premium usage ahead of a downgrade.
Two things don't have that same protection yet, and we wrote them up rather than silently patching them: the number of computers a company is allowed to build on, and how many teammates can be invited. Computers already behave reasonably — once you're over your new plan's limit, you're blocked from adding more, though anything already registered under the old plan quietly keeps working. Teammate invites have no such check at all right now — a company can invite more people than their plan allows on any plan, not just after a downgrade, because invites go straight to our login provider and never pass through our own servers to be checked. That one needs new plumbing to fix properly (listening for invite events from the login provider and removing anyone who pushes a company over its limit), so it's logged as prioritized follow-up work rather than rushed in.
 — 2026-08-09

## Recompiling a workflow now spends a build credit, not the editing budget — 2026-08-09
If you re-recorded a workflow and then rebuilt it, that rebuild was quietly free against your monthly build allowance and instead ate into your separate editing budget — the same pool meant for small fixes like re-pointing a button after a page redesign. That was backwards: rebuilding a whole workflow is real, expensive work, the same as building it the first time. Now every rebuild, first time or a repeat, spends one build credit, exactly like the first build always did. Fixing or re-targeting a single step by hand still uses the separate editing budget, unchanged.
 — 2026-08-09

## Closed two ways a free trial could quietly turn into free, unlimited distribution — 2026-08-09
Yesterday's change limited a free trial to one computer for building — but that limit stopped at the building step. Two gaps meant a copy of what got built could still travel further than intended. First: the installer file itself had no memory of which computer it came from, so it could be copied and run anywhere, on any number of machines, forever. Second, and more important: even after a company's plan changed, our servers kept sending their workflow updates out to every customer who already had the software installed, with no check at all — like a magazine that keeps mailing new issues to old subscribers even after the subscription was cancelled.
Both are fixed now. A trial installer is locked to the one computer it was built on and simply won't run anywhere else. And our servers now check, every time an update goes out, whether the sending company's current plan is actually allowed to reach outside customers — a free trial gets zero further updates sent to anyone but its own one registered computer, full stop, even if that company was previously on a paid plan and downgraded. As part of this we also loosened one existing rule: the mid-tier "Starter" plan can now reach unlimited customer computers (it was previously capped to internal use only), since its existing monthly limit on how many times a workflow can be rebuilt already keeps its usage in check without a separate machine cap.
 — 2026-08-09

## Rebuilt our pricing and our pitch around one growth story instead of two confusing ones — 2026-08-08
After a demo with a potential customer, we got hard but useful feedback: we couldn't clearly say who our product is for, and our pricing and website were carrying features for two different kinds of customer at once, which made the whole thing feel muddled. We fixed the story first, then rebuilt the pricing and the software underneath it to match. The new story is one ladder, not two products: a free trial lets someone prove the tool works on one computer, a paid plan lets a team run it across their whole organization, and the top plan lets a company ship it out to their own customers, with their own branding. Every price, every limit, and every "you need to upgrade for that" message on the website and in the app now comes from that same ladder, so nothing can drift out of sync with what we actually charge for.

Along the way we removed an old limit on how many products a company could publish (there's no longer a cap), and replaced it with a limit on how many computers a trial account can be used from — a much better way to stop someone from running a free trial forever. We also added the option, for our largest customers, to plug in their own AI provider account instead of using ours, which matters to banks and other companies with strict rules about where their data can go. All of the pricing pages, help pages, and internal engineering guides were rewritten to match, so nobody on the team or in support is working from an old, out-of-date price sheet.
 — 2026-08-08


## Brought the handbooks back in line with how the product actually works — 2026-08-07
Our internal documentation had drifted behind the software in a few places, and one section was describing a mechanism we removed weeks ago — the equivalent of a manual still explaining the old model's dashboard. The self-update chapter now matches reality: an installed copy picks up its own improvements on the launch that downloads them, rather than the one after. File uploads, which anyone can now record and replay, were documented nowhere and now have their own section explaining why the file itself has to be named at run time. The security and sales trackers were re-checked line by line: one old warning was corrected, one shipped fix was recorded, and the single remaining thing blocking an enterprise sale is confirmed unchanged.
 — 2026-08-07

## Rebuilt the customer dashboard into an operations control center — 2026-08-07
The dashboard customers see after logging in was written for engineers: a wall of counters and lists that told you what had broken, but never whether things were actually going well. It is now organised around the questions a manager or executive actually asks, each with its own page — is the platform healthy, what needs attention right now, which workflows are slipping, is the self-repair working, and what is all of this worth in saved time and money. A single health score sits at the top, and unlike most such scores it shows its own workings: the five things that produced it are listed underneath, so "we dropped four points" immediately becomes "our checks started failing." A live feed shows work as it happens, and everything is clickable down to a single run, step by step.
Two things were deliberately kept honest. The "hours saved" figure needs someone to say how long the task used to take a person — the software has no way of knowing that — so the assumption is printed right next to the number and anyone in charge can edit it, and it is labelled an estimate. Numbers that come purely from real activity are labelled measured, and kept separate. The dashboard also flags problems in plain language ("this new version is failing more often than the one before it — consider rolling back") using fixed rules rather than an AI, so every warning can be traced to a number and clicked through to the proof.
 — 2026-08-07

## Fixed the "free repairs" count being roughly double the real figure — 2026-08-07
While building the new dashboard, a number turned out to be wrong in a way that flattered us. When a workflow repairs itself, it tries the cheap local methods first and only calls on an AI model if those fail. We report how many repairs were handled free of charge — but the old count added up attempts rather than outcomes, so one step that tried two free methods was counted twice, and a step that tried a free method, failed, and then needed a paid AI call was still counted as free. On real data it reported 96 free repairs out of 64 total repairs, which is impossible on the face of it. It now counts what it says: steps that genuinely finished without ever needing a paid AI call. The same figure appears on two different pages, and both now read from one source so they can never disagree.
 — 2026-08-07

## Put the real demo video and product screenshots on the new homepage — 2026-08-07
The rebuilt homepage had blank spaces reserved for a demo video and pictures of the product. Both are now in: the video plays when you click it (it doesn't slow the page down before that), and four screenshots walk you through recording a task, the finished workflow, publishing it, and an AI actually running it. Two things needed care before publishing. The picture of the AI running a task also captured a sidebar full of private chat titles, so that strip was trimmed off. And a dashboard picture was left out on purpose, because it happened to show a bad run — a red warning and a 36% success rate from a test account — which is the last thing a cautious buyer should see.
 — 2026-08-07
