# Fix Log Archive — Index

`FIX.md` (repo root) is updated after every prompt per `CLAUDE.md`'s instructions. It's rotated
daily — each day's entries move to `FIX-<YYYY-MM-DD>.md` here — to keep the live file scannable.
This index lists every rotated file plus the live one.

| File | Entry count | Topics |
|---|---|---|
| [`FIX-2026-06-27.md`](FIX-2026-06-27.md) | 1 | Replay / Test Fix — Plain-English Writeup |
| [`FIX-2026-06-29.md`](FIX-2026-06-29.md) | 3 | "Deploy stops at search_repositories" — Element finding fix; Chromium Install Fix; README Deployment Guide |
| [`FIX-2026-06-30.md`](FIX-2026-06-30.md) | 8 | Fixed: T3/T4 self-healing recovery now actually works; Payment gateway switched from Razorpay to Cashfree; Skill pack modified to force Tier 3 + Tier 4 recovery escalation; Updated: cost model no longer includes LLM selector generation; Fixed: skill execution "got stuck" forever in Claude Desktop (but worked in Build Studio); Self-healing recovery made enterprise-ready: Tier 3/4 now actually work; CLAUDE.md updated to reflect major codebase changes; Fixed: runtime version numbers were hardcoded in package.json instead of being set automatically |
| [`FIX-2026-07-01.md`](FIX-2026-07-01.md) | 4 | Phase 2 production-readiness: billing limits, cache cleanup, drift warnings, friendlier errors; Finished Phase 1 (Architecture Consolidation); New feature: enterprise-grade auto-update system for the runtime; Fixed: resuming from the very first step silently skipped self-healing recovery |
| [`FIX-2026-07-02.md`](FIX-2026-07-02.md) | 8 | Fixed a production outage caused by a missing deploy setting; Closed the last 4 open security gaps (Medium severity); Refreshed the Security Gaps tracker; Refreshed the Sales-Blockers status doc; Fixed the Startup Sequence diagram not showing; Fully isolated Development and Production environments; Added a strategy write-up for TwelveLabs video understanding; Fixed Cashfree subscription upgrades |
| [`FIX-2026-07-03.md`](FIX-2026-07-03.md) | 21 | Cleaned up the shared code library that both apps depend on; New companion doc: every problem with both the founder's and Fable's solutions, in plain language; Critical-analysis report: added Fable's own solutions section; Critical-analysis report: final clean rewrite with all twelve answers integrated; Critical-analysis report: answered the last open problem — silent wrong clicks and no undo; Critical-analysis report: eleventh founder answer — domain verification kills the bot-blocker risk; Critical-analysis report: tenth founder answer — MCP is open, Claude is the start, not the ceiling; Critical-analysis report: full rewrite into a founder-ready Q&A document; Critical-analysis report: answered the last open criticism — the official-connector squeeze; Critical-analysis report: expired logins are handled by design — user re-authenticates at next run; Critical-analysis report: the "one run at a time" limit is now marked largely fixable; Critical-analysis report: added the founders' answer to the "shrinking market" risk; Critical-analysis report: recorded the founders' strategy decision, Mac commitment, and added a build-forward plan; Phase 1 cleanup of the Build Studio's code; Cleaned up the Conxa Cloud dashboard and backend — no visible changes, just tidier code; Added a costed reality-check on code signing and SOC 2 to the critical-analysis report; Added the founders' fourth answer to the critical-analysis report: MCP setup is too hard for normal people; Updated the critical-analysis report with the founders' answers to three criticisms; Rewrote the critical-analysis report in plain language with real-world examples; Added a brutally honest outside-in review of Conxa's strategy and architecture; Fixed Build Studio crashing on every fresh install, and a login check that trusted stale sessions forever |
| [`FIX-2026-07-04.md`](FIX-2026-07-04.md) | 15 | Fixed (the real cause this time): "Build failed" right after a successful publish, in Dev mode; Fixed: building an installer crashed right after publishing, in Dev mode; Fixed: installer builder wrongly rejected a perfectly good NSIS install; Changed: publishing to a local dev cloud now actually publishes; Fixed: "Build failed" when clicking Build Installer in dev mode; Fixed: "requires runtime >=1.0.3, installed: 0.0.0-dev" when testing a plugin; Fixed (for real this time): "Skill not found" when testing a plugin; Fixed: testing a plugin in dev mode said "Skill not found"; Added shipping instructions for the runtime "host" program; Fixed: the "Add" dropdown in the Human Edit tab did nothing when clicked; Created the local dev config file (.env.dev); Fixed Record Login hanging forever after closing the browser; Fixed "Failed to save auth session: Cannot switch to a different thread"; Fixed "A recording is already active" getting permanently stuck; Fixed the flickering login recording window |
| [`FIX-2026-07-06.md`](FIX-2026-07-06.md) | 1 | New design proposal: a simpler, 3-step Build Studio workflow |
| [`FIX-2026-07-07.md`](FIX-2026-07-07.md) | 9 | Wrote the target-customer list: 50 named companies and a plan for landing the first ten; Round two of the enterprise-strategy discussion: found the places where clicking beats APIs even for giants; Wrote down the "how do the giants ever pay us" strategy as real backlog items; Started building the redesigned Build Studio: new sidebar, auto-build on approve, and a package inspector; Redrew the left-hand navigation in the Build Studio design proposal; Added "Build Installer" back as its own step, plus a full write-up of how installers actually work; Added a guided 3-step wizard for re-targeting a step's element; Rewrote the Build Studio redesign proposal end-to-end around the final, decided-on workflow; Wrote down the plan for adding decision-making ("brain") to workflows |
| [`FIX-2026-07-08.md`](FIX-2026-07-08.md) | 3 | Merged the Dashboard and Record pages into one; Turned the re-target wizard from a small popup into its own full page; Filled the empty strip at the top of every Build Studio screen with a breadcrumb |
| [`FIX-2026-07-09.md`](FIX-2026-07-09.md) | 31 | Made element recording more accurate, closed a blind spot in the drift dashboard, and hardened two recovery edge cases; Fixed the Playwright component and data flow diagrams; Fixed the SeleniumBase data flow diagram; Gave developers a way to test with the real downloaded runtime instead of the local source code; Documented seven actionable improvements to runtime architecture; Removed the artificial "human speed" slowdown so skills run as fast as the page allows; Taught skills to handle "this pop-up sometimes shows up" instead of treating it as a broken step; Fixed the re-target wizard's new Validation step showing nothing at all for the common case; Stopped the "review selectors" step of re-targeting from also showing a second, conflicting Validation section; Kept a cookie banner from ever being "the" check, let people review checks by hand, and put the whole fleet's check-health on the dashboard; Made the runtime's post-action checks patient, and made them report everything they saw; Made the app actually check that a click or a typed value did what it was supposed to; Made the Human Edit screen look and feel like a finished, professional product; Merged in a big cloud-built change: Publish Skill Package is now the real release button; Closed a gap where drawing a new box could offer non-unique selectors again; Hid the junk options from "Review selectors"; Stopped "Review selectors" from calling verified options "Unverified"; Made the "Review selectors" step actually show the selectors; Stopped the re-target wizard from re-running the AI when nothing changed; Split the re-target wizard into three real pages; Fixed the real reason "Continue" on re-target step 1 went nowhere; Stopped the re-target wizard from skipping past "Review selectors"; Let re-target step 1 be reviewed and continued without forcing a redraw; Made every step-save error in Human Edit show plain language, not raw technical text; Made the re-target wizard's "Continue" button always visible on step 1; Fixed the real reason "Review" was still slow to load; Added a visible "Continue" step after drawing the box on the re-target page; Fixed a crash on the re-target page: "Tooltip must be used within TooltipProvider"; Stopped the Human Edit screen from scanning every saved skill every time you open one; Made "Publish Skill Package" the real, primary way to ship updates — and installers now travel light; Fixed the UI-TARS paper diagrams |
| [`FIX-2026-07-10.md`](FIX-2026-07-10.md) | 13 | Recorder now notices what actually happened after a click, and flags cookie-banner-style pop-ups so they don't break replays; Moved "Recording screenshots" off the Human Edit top bar into the Pick Element step; Switched the fix-log archive from monthly to daily files, and split the two existing monthly archives into one file per day; Fixed the "Workflow plan" and "Diagnostics" pop-ups being tiny and cutting off their content; Tidied the Human Edit top toolbar; Built the Human Edit redesign the earlier audit recommended, plus let people build "if this appears, do that" conditions; Wrote a concrete "what to build next in the recorder" recommendation; Fact-checked the recording-subsystem design doc against the real code; Audited what the Human Edit page shows vs. what a skill package actually contains; Showed reviewers the "durability score" behind each selector; Fixed "no usable selector" error when re-targeting an element by drawing a box; Fixed ugly step labels in the workflow step list; Added a "show all recording screenshots" button to the Human Edit screenshot picker |
| [`FIX-2026-07-11.md`](FIX-2026-07-11.md) | 10 | Explained why the Paxel upload command failed on Windows; Local Runtime testing now works exactly the same way as a real customer's install; Removed two dev scripts and a leftover code path nobody needed anymore; Local development now has a real build-and-replace workflow for the Runtime; Updated the compiler design doc to say what's actually built vs. still just planned; New script to build and test the real packaged runtime locally; "Test Skill" in Dev now always uses your local runtime code (closed the last two fallback paths); "Test Skill" now uses your local runtime code by default in Dev, plus a crash-on-load fix; Fixed "Test Skill" crashing in dev, and added a way to test local runtime changes before release; Wrote down the design rules for both apps' look and feel |
| [`FIX-2026-07-12.md`](FIX-2026-07-12.md) | 3 | Human Edit now shows what "success" means for a step, and step names stop being vague; Made self-healing recovery safer — it now double-checks itself instead of guessing on outdated information; Two more follow-on bugs from the local runtime testing workflow, both fixed |
| [`FIX-2026-07-13.md`](FIX-2026-07-13.md) | 4 | One more leak found from checking the recorder fix on a real workflow — an internal ID string showing up as a fake "page text" check; Found why "Validation" still showed up empty after the prior day's fix — the real bug was in recording, not compiling; Fixed nonsense "expected outcome" checks, and started actually using what gets recorded (entry appears twice in-source under a duplicated heading) |
| [`FIX-2026-07-14.md`](FIX-2026-07-14.md) | 5 | Fixed "Chromium installation failed (code 1)" with no details when installing a dev build; Cleaned up the Build Installer page and fixed a package showing as "No package"; Fixed workflow runs crashing on the "scroll" step with `ReferenceError: ak is not defined`; Found and fixed why some step edits (like a "Value" field) silently didn't save; Fixed the "re-target" edit wizard losing or mixing up changes between steps |
| [`FIX-2026-07-15.md`](FIX-2026-07-15.md) | 4 | Fixed the recorder silently cutting off a HubSpot workflow recording mid-session; Fixed "Record Workflow" popup getting permanently stuck after recording; Fixed the compile/recompile screen looking frozen during "Generate selectors"; Fixed recorded steps inside iframes silently disappearing at compile time (e.g. HubSpot workflows) |
| [`FIX-2026-07-16.md`](FIX-2026-07-16.md) | 17 | Confirmed fixed, end to end — a real HubSpot form field typed inside a panel now records, saves, compiles, and shows up correctly; Found the actual cause of the missing iframe fields — a self-inflicted deadlock in how we reported them; Added a marker around the URL sweep so the next freeze points at one exact call; Cleaned up the browser comparison note so it matches the recorder fix; Widening the grace period wasn't the whole fix either — added a finer trace to pinpoint the exact freeze; The grace period was too short — widened it based on real timing evidence; Found the real freeze point: checking too early, right as a panel is still settling in; Confirmed the tracking script really does attach inside the Create Contact panel — found a likely new culprit; The retry fix worked, but typing is still not captured — one more diagnostic added; Found and fixed why typing into the Create Contact panel was never captured; Confirmed the Create Contact panel is a real, healthy iframe; still tracking down why we can't get into it; More logging: caught the recorder momentarily "losing track" of the browser, still don't know why yet; Added logging to pin down why a HubSpot form's iframe is invisible to the recorder; Fixed typed form fields inside an iframe going completely uncaptured (HubSpot "Create a Contact"); Removed browser-close as the thing that finishes a recording; Widened the recorder's grace period further after a real test proved 8 seconds still wasn't enough; Fixed "Cancel" during a workflow recording leaving Build Studio stuck saying a recording is already in progress |
| [`FIX-2026-07-17.md`](FIX-2026-07-17.md) | 1 | Made the runtime actually handle steps that live inside a panel, not just record and show them |
| [`FIX-2026-07-20.md`](FIX-2026-07-20.md) | 2 | Installing a company's skill package no longer force-closes your Claude Code terminal session; Wrote the plan for making our installer work with every AI assistant, not just Claude |
| [`FIX-2026-07-21.md`](FIX-2026-07-21.md) | 2 | Conxa can now configure itself into 24 different AI assistants, not just Claude — and the dangling-connection bug from yesterday is fixed for real; Caught two build-pipeline gaps that would have made yesterday's feature quietly not work for real customers |
| [`FIX-2026-07-23.md`](FIX-2026-07-23.md) | 4 | Fixed several bugs in the new "fill-in-the-blank" skill variables feature, and caught a scare with leaked passwords before it went anywhere; Audited the hand-off between the three build stages and found edits that quietly never reach the test run; Audited the whole path from "recording compiled" to "workflow hand-edited" and found two data-loss traps; Fixed the edit-then-run gaps the audit found: renamed steps now really take effect, and variables stay consistent |
| [`FIX-2026-07-24.md`](FIX-2026-07-24.md) | 1 | Fixed the remaining problems from this week's "compile → edit → test" hand-off audit |
| [`FIX-2026-07-25.md`](FIX-2026-07-25.md) | 3 | Made the runtime work with a local test cloud, and fixed a crash that stopped it starting at all; Fixed a packing mistake that made the customer installer fail right after "Setup complete!"; Fixed a crash when drawing a new box around an element in the step editor |
| [`FIX-2026-07-30.md`](FIX-2026-07-30.md) | 7 | Fixed a rare "reinstall the app" error when running a workflow test; Corrected wrong instructions in the "Record Workflow" screen; Fixed Build Studio skipping the login screen on a fresh install; Sped up first-time setup by downloading things at the same time instead of one after another; Fixed customer installers sometimes shipping months-old app code even after a fix was made; Fixed a second, related case of installers shipping an out-of-date core program file; Fixed the cloud dashboard crashing when clicking "Rebuild" on a test (non-live) release channel |
| [`FIX-2026-07-31.md`](FIX-2026-07-31.md) | 7 | Fixed the "reinstall needed" test error for real this time; Explained in the product document why customer updates reach people quickly; Updated the product definition document to match what Conxa actually does today; Redesigned the Input Variables screen and added an "Optional" setting; Made a test-engine update failure fail loudly instead of quietly breaking the next test; Sped up the "Apply" button in Human Edit's re-targeting tool; Fixed installed customer apps being unable to download their skills or check for updates |
| [`FIX-2026-08-06.md`](FIX-2026-08-06.md) | 4 | Rebuilt the homepage so visitors understand what we do in the first 30 seconds; Fixed an upload failing when the file's folder name has spaces in it; Fixed a finished upload workflow freezing the browser instead of uploading the file; Fixed "Choose File" doing nothing while recording a workflow |
| [`FIX-2026-08-04.md`](FIX-2026-08-04.md) | 4 | Turned the pre-release safety rehearsal back on — and it caught a real problem immediately; Anyone not part of a company team was locked out of publishing; Two automated safety checks had drifted and were quietly failing; One stuck picture-taking step used to spoil an entire recording's snapshots |
| [`FIX-2026-08-03.md`](FIX-2026-08-03.md) | 1 | App updates now reach customers on their very next launch, not the one after |
| [`FIX-2026-08-02.md`](FIX-2026-08-02.md) | 1 | Fixed the login window rejecting a successful sign-in |
| [`FIX-2026-08-01.md`](FIX-2026-08-01.md) | 3 | Fixed skill pack publishing rejecting normal-sized packages; Publishing failures now say why, instead of just "try again"; Fixed the "review this step" screen quietly weakening a working step every time you opened it |
| [`FIX-2026-08-07.md`](FIX-2026-08-07.md) | 4 | Brought the handbooks back in line with how the product actually works; Rebuilt the customer dashboard into an operations control center; Fixed the "free repairs" count being roughly double the real figure; Put the real demo video and product screenshots on the new homepage |
| [`FIX-2026-08-08.md`](FIX-2026-08-08.md) | 1 | Rebuilt our pricing and our pitch around one growth story instead of two confusing ones |
| [`FIX-2026-08-09.md`](FIX-2026-08-09.md) | 7 | Free installers no longer lock to one computer, and their name and icon now depend on the plan; Developers can skip the sign-in screen when working on the Build Studio locally; Support can now grant someone a paid plan for free, with an optional expiry date; Downgrading a plan now locks the extra workflows you built while on the higher plan; Checked what happens when someone downgrades their plan after using the higher tier for a while; Recompiling a workflow now spends a build credit, not the editing budget; Closed two ways a free trial could quietly turn into free, unlimited distribution |
| [`FIX-2026-08-12.md`](FIX-2026-08-12.md) | 11 | Each customer's downloaded skills are now sorted into their group's own folder; Corrected the runtime startup guide; Workflows are now organized into groups, and logins are set up once per group; Removed the Record page and restructured the Studio around Workflows; Added an honest side-by-side against the alternatives; Anyone clicking "Pricing" was being asked to sign in first; Rebuilt the homepage around what we sell and what it costs; Rewrote the whole middle of the product handbook — what we sell, what it does, who we beat, how we make money; Wrote a proper "who we sell to" section, including who we should turn away; Rewrote the "what you get" section of the product handbook; Rewrote the "problem" section of the product handbook so it lands with the people we sell to |
| [`FIX-2026-08-13.md`](FIX-2026-08-13.md) | 1 | Opening a group now shows everything about its workflows on one screen |
| [`FIX-2026-08-14.md`](FIX-2026-08-14.md) | 1 | Wrote a free test plan for the "does it handle long, complicated tasks?" question |
| [`FIX-2026-08-15.md`](FIX-2026-08-15.md) | 13 | Fixed "Invalid bundle name" blocking every package build after approving Human Edit; Fixed vision-based element detection quietly going dark during long compiles; Fixed a technical warning that appeared in the background every time a confirmation popup opened; Fixed compiling a workflow freezing forever with no error; Stopped a wrong "please sign in" message from blocking compiles in developer mode; Fixed downloaded files overwriting each other, and a failed task making every later one worse; Redesigned the Workflows and group screens, and stopped compiles from being paid for twice; Planned a new "ask Claude mid-task" step, and found we already built most of it; Wrote a "try to break it on purpose" test plan, and found seven weak spots before a customer could; Added a "try to break it on purpose" test plan, and found seven weak spots before a customer could; Fixed a broken wildcard, replaced raw web addresses with app names, and added an automatic "are you still logged in?" check; A recording can now open a new tab, work there, and come back — and it actually replays correctly; Fixed a leftover browser tab quietly staying open after a background task finished |
| [`FIX-2026-08-16.md`](FIX-2026-08-16.md) | 10 | When a workflow downloads a zip, it now unpacks it right away; "Upload all the files I just downloaded" now works even picked all at once; A workflow that visits a second website mid-run no longer hits a surprise login screen; Recording a workflow no longer quietly logs you out of a saved website; A workflow no longer waits on logins it doesn't need, just because a groupmate does; Uploading 20 files at once now works — just point the workflow at a folder; Giving a folder to a page that only takes one file now says so plainly; Found that picking 20 files at once to upload only ever sends one of them; Fixed a new tab pausing for up to two minutes before it typed the web address; Fixed a two-tab workflow replaying on the wrong page and giving up too soon on slow websites |
| [`FIX-2026-08-17.md`](FIX-2026-08-17.md) | 7 | The "Choose File" window now opens in the right folder every time; Logins are now checked before a workflow starts, not in the middle of it; Files you download while recording a workflow now land somewhere you can actually find; The `agent` command now has a reliable fallback on PATH; Cursor, codebase memory, and Ponytail are set up cleanly on this machine; A downloaded zip file now automatically unpacks itself before uploading; Downloaded files now get their own private, self-cleaning folder for each run |
| [`FIX-2026-08-18.md`](FIX-2026-08-18.md) | 4 | Fixed the "Testing os picker" compile failure; The "Testing os picker" compile failure is now tracked as top priority; Uploads from downloaded or extracted files no longer ask for a file path; Downloaded files now show up in the file-picker window during recording |
| [`FIX-2026-08-19.md`](FIX-2026-08-19.md) | 5 | Stopped the workflow test screen from asking for a "downloaded file dir" it should already know; Fixed uploads that download-then-upload a file sometimes asking for a file path anyway; Publishing a skill pack update is now a real, undoable release; Publishing one automation no longer waits on — or gets blocked by — every other automation; Publishing an update from the desktop app no longer sends it straight to customers |
| [`FIX-2026-08-20.md`](FIX-2026-08-20.md) | 9 | Fixed group links on the Skill Packages page taking a wrong detour; New installs now download the skill package during setup, not on first use; Removed the leftover "compile credits" counter from the Publish page; Build Installer page redesigned into two columns; Build Installer page now asks for an installer name; Publishing no longer fails when a workflow has a very long name; Signing in to an application in Build Studio no longer gets stuck waiting forever; Application cards on the Workflows page are easier to read; Added a one-click recheck button to connected applications; Skill Packages on the web now shows your folders, not the whole workspace as one box; Creating a group in Build Studio now shows it on Conxa Cloud immediately; The dev launcher now starts Build Studio with one double-click; Build Studio no longer fills itself with sample data on startup |
| [`FIX-2026-08-21.md`](FIX-2026-08-21.md) | 10 | The company domain you type in Build Studio now also updates the Cloud Dashboard; Fixed skills failing instantly when run through the AI assistant; Wrote down the point where Conxa comes full circle; The installer now asks for your company's domain, and uses it everywhere it shows your name; Settled the one rule that decides how Conxa grows; Explained why our long-term plan has to happen in that order; Wrote down where Conxa is going long-term, and made room for people in our automations; Fixed "Build Installer" crashing with a missing-argument error; The empty "Default" folder is gone, and the Workflows top bars now read as a proper toolbar; Simplified how a company's automations are organized behind the scenes |
| [`FIX-2026-08-22.md`](FIX-2026-08-22.md) | 49 | A confusing "no address configured" error was hiding the real problem; Every failed automated task used to crash instead of reporting a clean error; The runtime program that customers install was silently missing all its parts; Two workflows that both touch the same outside website now wait their turn; The assistant no longer tells you to cancel someone else's workflow; Fixed the crash that broke the automated test gate against host-v2.0.0; Double-checked the recent runtime rebuild for mistakes; Dev and production runtime builds now run the exact same recipe; Stopped shipping a useless copy of the launcher inside every app update; Full health check of the cloud dashboard's website code; A guide explaining exactly what gets installed on customer computers; Added a hands-on stress-test guide you can follow yourself; The "Workflow plan" panel no longer comes up empty on a first compile; All pending work organized into clean, labeled commits; Team seat limits are now actually enforced, not just displayed; New workspaces are now asked for their company domain right at signup; Added optional FreeLLMAPI support to the AI provider pool; Compile no longer quietly downgrades to weaker element-finding when the AI is rate-limited; Admins can now actually grant paid plans manually; The credit add-on is now a ladder of four sizes instead of one; Customers can now manage their own LLM key, credit add-on, and see their trial countdown; Shortened page descriptions and added an info icon; Fixed page titles getting cut off in the merged top bar; Merged the two top bars on the dashboard into one; Published the Build Studio how-to guide on the public docs website; Added a way to see every machine running Conxa's software; Fixed Build Studio forgetting your name and email after signing in; Fixed Build Studio crashing right after install for some accounts; Folder-shaped groups on the Cloud Skill Packages page; Wrote a plain-English guide to how self-healing works (the 4 recovery tiers); Wrote a step-by-step manual testing guide for plan limits; Production readiness manual testing guide; P0 item: run the full manual testing suite from docs/testing/; Synced cost model doc with PRD and real code pricing; Added the missing Cashfree compile add-on settings to backend env files; Added future-horizon revenue projections to the cost model; Read-only refactor audit of the cloud backend (no code changed); Report on the shared conxa-core package (no code changed); Full refactor audit report for the cloud backend (no code changed); Refactor audit of conxa-builder (report only, no code changed); Refactor audit of the runtime folder (report only, no code changed); Compile add-on packs changed from monthly subscriptions to one-time purchases; Edge-case audit (full codebase); Implemented the runtime refactor plan (safety nets, correctness fixes, file splits); Committed all pending non-runtime work in labeled commits; Runtime sources split into host/ and app/ folders (release boundary made physical); Added P0 item: run multiple workflows at the same time reliably; Closed the last two runtime-refactor follow-ups (register orchestrators + marker-block editing); Removed the recovery-ceiling gate from the app release pipeline |
| [`FIX-2026-08-23.md`](FIX-2026-08-23.md) | 9 | The file picker that pops up while recording now actually shows up in front; Switching back to the first tab now actually switches; A compile would silently freeze for eight minutes, an on/off switch pointed at the wrong place, and a real error hid behind a generic one; A large recording would fail to compile with a wall of "server error" messages; A workflow that uses two websites now shows both name tags; Added a to-do: workflow checks on the Workflows page should run in parallel; Fixed two cloud issues from the backlog (CLOUD-19 and CLOUD-20); Fixed: "Go to URL" steps in Human Edit no longer show the Pick Element screen; New: you can now record over a workflow's old recording, and the old one is kept safe |
| [`FIX-2026-08-24.md`](FIX-2026-08-24.md) | 6 | A test run could sit frozen for over a minute right after the first step started; The Test Skill page now shows exactly where a slow test is spending its time; Fixed the main cause of slow "Run Test" clicks — starting a brand-new helper program from scratch every time; The test log now shows how long each individual step takes, not just the overall run; Fixed a case where a code change wouldn't show up in a test run until minutes later; Found and fixed the real reason "Run Test" could sit blank for up to half a minute before doing anything |
| [`FIX-2026-08-25.md`](FIX-2026-08-25.md) | 27 | Fixed a leftover-file mixup when one workflow chains several downloads and uploads; Wrote up a real safety gap: our automatic repair attempts can act on the page more than once; The upload file picker now pops up in front of the recording window, not behind it; A hands-on test drill for the self-healing tiers (Tier 2 and Tier 3); Self-healing recovery now works in two smarter steps instead of one big expensive one; The big multi-tab, cross-domain test workflow now passes end to end; The "Ready to Package" light now actually turns green after publishing; Recording now captures the browser's Back and Forward buttons; The login double-check before a test run now takes seconds instead of about a minute; Changed: "Ready to Package" now stays amber until the skill is actually published; Homepage hero headline update; Pricing section no longer depends on the backend; Homepage comparison table rewritten in plain business language; Comparison table axes swapped; New harder test plan for Conxa (EXEC-11); The marketing homepage got a readability and tidy-up pass; EXEC-11 collapsed into one mega-workflow runbook; The homepage now has one navigation bar instead of two; Added governance-enforcement item (PROD-18) and hard-mode testing item (TEST-12) to TODO.md; Hard-mode testing consolidated into one self-contained document; New test workflow for conditional pop-up handling (EXEC-1); Explained how the Workflow Plan in Human Edit is created; Added AV-5 test: pop-up shows up at replay even though it was never recorded; The Workflow Plan and each step's intent now come from one AI call; The runtime now knows famous pop-up buttons (and remembers what works on each site); Manual walkthrough written (and it caught two real bugs) for the pop-up cheat sheet; Human Edit now shows each step in plain English (matching the Workflow plan) |
| [`FIX-2026-08-26.md`](FIX-2026-08-26.md) | 16 | Running a saved workflow could freeze forever on a website's pop-up box; Answering a pop-up box left a ghost copy of it behind; Pop-up boxes were vanishing before you could answer them; Stopped recordings picking up pointless "hover here" steps; Fixed steps that point at a picture or icon being named after the text next to them; Retyping the address bar mid-recording is now captured; Two silent gates were still throwing away those address-bar steps; Fixed the compile timer counting up instead of down; A broken drag-and-drop step no longer blocks the whole package build; Fixed the AI plan-builder rejecting a working AI provider as unusable; Fixed recordings that need a mouse-hover to reveal what you click next; All five testing guides merged into two easy-to-navigate documents; Checked whether huge (100+ step) recordings break the workflow-plan AI call; Conxa can now run your skills on a schedule, all by itself; App-layer build manifest guard fix (new PROD-5/scheduler modules); One-workflow test runbook |
| [`FIX-2026-08-29.md`](FIX-2026-08-29.md) | 7 | Local app updates would have shipped without two new safety files; A deleted run's records can no longer vanish without a trace, and companies can now block runs outside allowed hours; Delete and Pay buttons can no longer be tricked into acting on the wrong row; Build Studio now has a real licence, and you have to accept it before using the app; Accepting the Build Studio terms is now recorded on our servers, not just on the customer's PC; A step editor tucked inside conditional branches still showed computer-speak instead of plain English; The Workflow Test box asked for answers that weren't actually needed |
| [`FIX-2026-08-30.md`](FIX-2026-08-30.md) | 6 | The compile screen had two separate title bars saying the same thing; A "date of birth" style calendar with dropdown month/year pickers turned one date into a mess of junk settings; The Workflows page buttons now say "Re-record" and "Re-compile" once a workflow already has both; The group workflows screen scrolled as one giant page instead of just the long lists inside it; Recording a calendar date picker now actually works when the skill runs again later; A manually typed web address could vanish from a recording if it followed a button that didn't actually go anywhere |
| [`FIX-2026-08-31.md`](FIX-2026-08-31.md) | 1 | Recording a multiple-choice question (like picking a gender or a country) now actually remembers every choice, not just the one you clicked |
| [`FIX-2026-09-01.md`](FIX-2026-09-01.md) | 18 | Workflow-E now runs start to finish; Fixed a calendar being mistaken for one of its own dates; Fixed the recorder throwing away most of what it noticed; Fixed dates in calendars being recorded a day early; Fixed clicking a day in a calendar not being recorded at all; Fixed a background accessibility check that never once ran; Fixed workflows failing on dropdowns that have no name of their own; Stopped the system from quietly rejecting the right answer; Taught workflows how to reopen a dropdown before picking from it; Stopped the Build Studio developer console from flooding with error messages every time it restarted; Found and fixed why a recorded date-of-birth picker failed when tested; Fixed three smaller bugs in the calendar-picker feature found during the date-of-birth investigation; Recording now leaves hover-menu watching switched off unless you turn it on; The compile screen got stuck showing a failed compile forever, with no way to try again; Pune target list for the first three pilots; Centelon added to the top of the Pune target list; Wrote two scoped internship briefs for the incoming interns; Planned making Conxa run on its own, without needing a paid Claude or Codex account |
| [`FIX-2026-09-02.md`](FIX-2026-09-02.md) | 21 | Starter and Pro plans can each get their own AI provider, keys, and models; Risky steps can now repair themselves again, without ever picking a different button; Fixed a check that compared new answers against old ones; The workflow editor's health summary no longer points at the wrong step; Stopped one form field from being labelled with a different field's answer; Fixed a test that could never pass, no matter how long we waited; Confirmed that waiting longer would not have helped; Fixed the invisible "Recompile" button in the Build Studio; The dashboard now shows the same two repair stages as the product; Docs now call the two repair stages by their real names; Automations now switch back to the right tab after a link opens a new one; New tabs opened by a link are now recorded properly; Workflows that sign in as part of their job are no longer mistaken for expired logins; Local app rebuild now includes the screenshot store; Fixed a workflow blaming the wrong company when a login step failed; Fixed an ordinary button being mistaken for a length "record" that needed extra protection; Automations now stop guessing when a button moves; Repairs now get the full picture on the first try; Stopped telling Claude to look at a picture that was never sent; The "time saved" number on the Impact page now uses real recorded time, not a guess; Fixed a step that got another step's information by mistake |
| [`FIX-2026-09-03.md`](FIX-2026-09-03.md) | 9 | Taught the assistant to notice and clear away surprise pop-ups during a run; Proved a moved button can still be found by its label alone; Explained why the recorder pops up its own box for browser alerts instead of the browser's own box; Fixed a "you have to answer this pop-up twice" bug during recording; Fixed a delayed "welcome back" step during recording that made the recording browser look frozen; Finished the self-repair test drill on real, live websites; The self-repair test drill passed end to end, and the scoreboard moved up; A confusing "element not found" message during self-healing now explains itself instead; Ran the big single-recording test again and found two real gaps |
| [`FIX-2026-09-04.md`](FIX-2026-09-04.md) | 16 | Logged Conxa's biggest successful test run yet, and updated the test scoreboard; Fixed recordings on modern component-based websites losing track of button labels; Fixed a calendar picker freezing when asked to go back several months instead of forward; Wrote down a plan to cut the manual clean-up work after every recording; Fixed calendar-picker recordings that ignored a different requested date on replay; Fixed pop-up boxes freezing a run, and found why the typed answer sometimes vanished; Confirmed the pop-up-box fix actually works with a real re-test; Added a refresh button to every screen in Build Studio; Found and fixed the real, deeper cause of the "answer this pop-up twice" bug; Matched the run window to a modern dark desktop layout; Fixed Conxa Execute refusing to open in development; Opened a company window that can run skills without a paid chat app; Wrote down the plan for a Conxa app that runs skills without Claude; Added a Cancel button and a running clock to Build Studio's "Run Test"; Fixed a recorded click sometimes getting the wrong instructions baked in, including once with a password shape; Found and fixed the real cause of the "answer this pop-up twice" bug during recording |
| [`FIX-2026-09-05.md`](FIX-2026-09-05.md) | 7 | Removed skills from the left sidebar; Gave CONXA a Claude-style top bar and a personal account footer; Renamed Conxa Execute to CONXA in the app; Polished the Execute app's left sidebar; Gave the Execute chat app real sign-in, real monthly plans, and a memory that survives closing it; Built the online store for buying chat tokens in the Execute app; Logged a passing bulk file-transfer test, and noted it was a smaller test run than originally planned |
| `FIX.md` (live) | — | today's (2026-09-06) entries, not yet rotated |

**Rotation rule:** at the end of each day (or sooner if `FIX.md` exceeds ~800 lines), split by
each entry's own trailing `— YYYY-MM-DD` date — not a blind line-number cut, since entries are
not always strictly chronological top-to-bottom — into `FIX-<YYYY-MM-DD>.md`, add a row above,
and reset the live file to a short header pointing here.
