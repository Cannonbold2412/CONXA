# Fix Log Archive — Index

`FIX.md` (repo root) is updated after every prompt per `CLAUDE.md`'s instructions. It's rotated
daily — each day's entries move to `FIX-<YYYY-MM-DD>.md` here — to keep the live file scannable.
This index lists every rotated file plus the live one.

| File | Entry count | Topics |
|---|---|---|
| [`FIX-2026-06-27.md`](FIX-2026-06-27.md) | 1 | Replay / Test Fix — Plain-English Writeup |
| [`FIX-2026-06-29.md`](FIX-2026-06-29.md) | 3 | "Deploy stops at search_repositories" — Element finding fix; Chromium Install Fix; README Deployment Guide |
| [`FIX-2026-06-30.md`](FIX-2026-06-30.md) | 8 | Fixed: T3/T4 self-healing recovery now actually works; Payment gateway switched from Razorpay to Cashfree; Skill pack modified to force Tier 3 + Tier 4 recovery escalation; Updated: cost model no longer includes LLM selector generation; Fixed: skill execution "got stuck" forever in Claude Desktop (but worked in Build Studio); Self-healing recovery made enterprise-ready: Tier 3/4 now actually work; CLAUDE.md updated to reflect major codebase changes; Fixed: runtime version numbers were hardcoded in package.json instead of being set automatically |
| [`FIX-2026-07-01.md`](FIX-2026-07-01.md) | 4 | Phase 2 production-readiness: billing limits, cache cleanup, drift warnings, friendlier errors; Finished Phase 1 (Architecture Consolidation); New feature: enterprise-grade auto-update system for the runtime; Fixed: resuming from the very first step silently skipped self-healing recovery |
| [`FIX-2026-07-02.md`](FIX-2026-07-02.md) | 8 | Fixed a production outage caused by a missing deploy setting; Closed the last 4 open security gaps (Medium severity); Refreshed the Security Gaps tracker; Refreshed the Sales-Blockers status doc; Fixed the Startup Sequence diagram not showing; Fully isolated Development and Production environments; Added a strategy write-up for TwelveLabs video understanding; Fixed Cashfree subscription upgrades |
| [`FIX-2026-07-03.md`](FIX-2026-07-03.md) | 21 | Cleaned up the shared code library that both apps depend on; New companion doc: every problem with both the founder's and Fable's solutions, in plain language; Critical-analysis report: added Fable's own solutions section; Critical-analysis report: final clean rewrite with all twelve answers integrated; Critical-analysis report: answered the last open problem — silent wrong clicks and no undo; Critical-analysis report: eleventh founder answer — domain verification kills the bot-blocker risk; Critical-analysis report: tenth founder answer — MCP is open, Claude is the start, not the ceiling; Critical-analysis report: full rewrite into a founder-ready Q&A document; Critical-analysis report: answered the last open criticism — the official-connector squeeze; Critical-analysis report: expired logins are handled by design — user re-authenticates at next run; Critical-analysis report: the "one run at a time" limit is now marked largely fixable; Critical-analysis report: added the founders' answer to the "shrinking market" risk; Critical-analysis report: recorded the founders' strategy decision, Mac commitment, and added a build-forward plan; Phase 1 cleanup of the Build Studio's code; Cleaned up the Conxa Cloud dashboard and backend — no visible changes, just tidier code; Added a costed reality-check on code signing and SOC 2 to the critical-analysis report; Added the founders' fourth answer to the critical-analysis report: MCP setup is too hard for normal people; Updated the critical-analysis report with the founders' answers to three criticisms; Rewrote the critical-analysis report in plain language with real-world examples; Added a brutally honest outside-in review of Conxa's strategy and architecture; Fixed Build Studio crashing on every fresh install, and a login check that trusted stale sessions forever |
| [`FIX-2026-07-04.md`](FIX-2026-07-04.md) | 15 | Fixed (the real cause this time): "Build failed" right after a successful publish, in Dev mode; Fixed: building an installer crashed right after publishing, in Dev mode; Fixed: installer builder wrongly rejected a perfectly good NSIS install; Changed: publishing to a local dev cloud now actually publishes; Fixed: "Build failed" when clicking Build Installer in dev mode; Fixed: "requires runtime >=1.0.3, installed: 0.0.0-dev" when testing a plugin; Fixed (for real this time): "Skill not found" when testing a plugin; Fixed: testing a plugin in dev mode said "Skill not found"; Added shipping instructions for the runtime "host" program; Fixed: the "Add" dropdown in the Human Edit tab did nothing when clicked; Created the local dev config file (.env.dev); Fixed Record Login hanging forever after closing the browser; Fixed "Failed to save auth session: Cannot switch to a different thread"; Fixed "A recording is already active" getting permanently stuck; Fixed the flickering login recording window |
| [`FIX-2026-07-06.md`](FIX-2026-07-06.md) | 1 | New design proposal: a simpler, 3-step Build Studio workflow |
| [`FIX-2026-07-07.md`](FIX-2026-07-07.md) | 9 | Wrote the target-customer list: 50 named companies and a plan for landing the first ten; Round two of the enterprise-strategy discussion: found the places where clicking beats APIs even for giants; Wrote down the "how do the giants ever pay us" strategy as real backlog items; Started building the redesigned Build Studio: new sidebar, auto-build on approve, and a package inspector; Redrew the left-hand navigation in the Build Studio design proposal; Added "Build Installer" back as its own step, plus a full write-up of how installers actually work; Added a guided 3-step wizard for re-targeting a step's element; Rewrote the Build Studio redesign proposal end-to-end around the final, decided-on workflow; Wrote down the plan for adding decision-making ("brain") to workflows |
| [`FIX-2026-07-08.md`](FIX-2026-07-08.md) | 3 | Merged the Dashboard and Record pages into one; Turned the re-target wizard from a small popup into its own full page; Filled the empty strip at the top of every Build Studio screen with a breadcrumb |
| [`FIX-2026-07-09.md`](FIX-2026-07-09.md) | 31 | Made element recording more accurate, closed a blind spot in the drift dashboard, and hardened two recovery edge cases; Fixed the Playwright component and data flow diagrams; Fixed the SeleniumBase data flow diagram; Gave developers a way to test with the real downloaded runtime instead of the local source code; Documented seven actionable improvements to runtime architecture; Removed the artificial "human speed" slowdown so skills run as fast as the page allows; Taught skills to handle "this pop-up sometimes shows up" instead of treating it as a broken step; Fixed the re-target wizard's new Validation step showing nothing at all for the common case; Stopped the "review selectors" step of re-targeting from also showing a second, conflicting Validation section; Kept a cookie banner from ever being "the" check, let people review checks by hand, and put the whole fleet's check-health on the dashboard; Made the runtime's post-action checks patient, and made them report everything they saw; Made the app actually check that a click or a typed value did what it was supposed to; Made the Human Edit screen look and feel like a finished, professional product; Merged in a big cloud-built change: Publish Skill Package is now the real release button; Closed a gap where drawing a new box could offer non-unique selectors again; Hid the junk options from "Review selectors"; Stopped "Review selectors" from calling verified options "Unverified"; Made the "Review selectors" step actually show the selectors; Stopped the re-target wizard from re-running the AI when nothing changed; Split the re-target wizard into three real pages; Fixed the real reason "Continue" on re-target step 1 went nowhere; Stopped the re-target wizard from skipping past "Review selectors"; Let re-target step 1 be reviewed and continued without forcing a redraw; Made every step-save error in Human Edit show plain language, not raw technical text; Made the re-target wizard's "Continue" button always visible on step 1; Fixed the real reason "Review" was still slow to load; Added a visible "Continue" step after drawing the box on the re-target page; Fixed a crash on the re-target page: "Tooltip must be used within TooltipProvider"; Stopped the Human Edit screen from scanning every saved skill every time you open one; Made "Publish Skill Package" the real, primary way to ship updates — and installers now travel light; Fixed the UI-TARS paper diagrams |
| [`FIX-2026-07-10.md`](FIX-2026-07-10.md) | 13 | Recorder now notices what actually happened after a click, and flags cookie-banner-style pop-ups so they don't break replays; Moved "Recording screenshots" off the Human Edit top bar into the Pick Element step; Switched the fix-log archive from monthly to daily files, and split the two existing monthly archives into one file per day; Fixed the "Workflow plan" and "Diagnostics" pop-ups being tiny and cutting off their content; Tidied the Human Edit top toolbar; Built the Human Edit redesign the earlier audit recommended, plus let people build "if this appears, do that" conditions; Wrote a concrete "what to build next in the recorder" recommendation; Fact-checked the recording-subsystem design doc against the real code; Audited what the Human Edit page shows vs. what a skill package actually contains; Showed reviewers the "durability score" behind each selector; Fixed "no usable selector" error when re-targeting an element by drawing a box; Fixed ugly step labels in the workflow step list; Added a "show all recording screenshots" button to the Human Edit screenshot picker |
| [`FIX-2026-07-11.md`](FIX-2026-07-11.md) | 10 | Explained why the Paxel upload command failed on Windows; Local Runtime testing now works exactly the same way as a real customer's install; Removed two dev scripts and a leftover code path nobody needed anymore; Local development now has a real build-and-replace workflow for the Runtime; Updated the compiler design doc to say what's actually built vs. still just planned; New script to build and test the real packaged runtime locally; "Test Skill" in Dev now always uses your local runtime code (closed the last two fallback paths); "Test Skill" now uses your local runtime code by default in Dev, plus a crash-on-load fix; Fixed "Test Skill" crashing in dev, and added a way to test local runtime changes before release; Wrote down the design rules for both apps' look and feel |
| [`FIX-2026-07-12.md`](FIX-2026-07-12.md) | 3 | Human Edit now shows what "success" means for a step, and step names stop being vague; Made self-healing recovery safer — it now double-checks itself instead of guessing on outdated information; Two more follow-on bugs from the local runtime testing workflow, both fixed |
| [`FIX-2026-07-13.md`](FIX-2026-07-13.md) | 4 | One more leak found from checking the recorder fix on a real workflow — an internal ID string showing up as a fake "page text" check; Found why "Validation" still showed up empty after the prior day's fix — the real bug was in recording, not compiling; Fixed nonsense "expected outcome" checks, and started actually using what gets recorded (entry appears twice in-source under a duplicated heading) |
| [`FIX-2026-07-14.md`](FIX-2026-07-14.md) | 5 | Fixed "Chromium installation failed (code 1)" with no details when installing a dev build; Cleaned up the Build Installer page and fixed a package showing as "No package"; Fixed workflow runs crashing on the "scroll" step with `ReferenceError: ak is not defined`; Found and fixed why some step edits (like a "Value" field) silently didn't save; Fixed the "re-target" edit wizard losing or mixing up changes between steps |
| [`FIX-2026-07-15.md`](FIX-2026-07-15.md) | 4 | Fixed the recorder silently cutting off a HubSpot workflow recording mid-session; Fixed "Record Workflow" popup getting permanently stuck after recording; Fixed the compile/recompile screen looking frozen during "Generate selectors"; Fixed recorded steps inside iframes silently disappearing at compile time (e.g. HubSpot workflows) |
| `FIX.md` (live) | — | today's entries, not yet rotated |

**Rotation rule:** at the end of each day (or sooner if `FIX.md` exceeds ~800 lines), split by
each entry's own trailing `— YYYY-MM-DD` date — not a blind line-number cut, since entries are
not always strictly chronological top-to-bottom — into `FIX-<YYYY-MM-DD>.md`, add a row above,
and reset the live file to a short header pointing here.
